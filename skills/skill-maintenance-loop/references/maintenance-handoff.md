# Context-handoff

Read when the current product cannot access or verify the authoritative state.

Preserve only goal and success gate, verified state, relevant paths, commits, hashes, backups or IDs, exact limitation, next reproducible action, rollback and evidence expected from the destination context. Use handoff only when continuing locally would require guessing or disproportionate infrastructure. Do not use it to avoid an investigation possible in the current context.
