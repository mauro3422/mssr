# MSSR roadmap

## Release boundary (0.2.8)

- `0.2.7` delivered the portable route-closure evaluator and the success-outcome
  gate. `0.2.8` records its release boundary, project state, and package scope;
  it does not add a new routing algorithm or relax a closure obligation.
- The first-party package now contains five canonical skill trees, a versioned
  reserved-name manifest, native/Codex/OpenCode provider discovery, opt-in Codex
  installation, and source-level conformance tests. Bridge `0.6.88` completed a
  controlled full restart and live catalog/version readback on 2026-08-13.
- Historical learning remains `observe-only` with `routingInfluence=false`.
  Dataset audit, replay/holdout, calibration, shadow evaluation, a versioned
  feature flag, and tested rollback remain separate future gates.

## Release boundary (0.2.10)

- `0.2.9` published the portable Context Messages v1 contract and cross-host
  selection parity. `0.2.10` adds the Context Plane phase 2 portable core:
  strict producers, a bounded repository collector over ADR/incident/changelog/
  PROJECT_* facts plus supplied Git/provider receipts, evidence freshness
  revalidation, and a durable explicit-ack advisory-only JSON inbox.
- Host delivery integration — draining the inbox or piggybacking receipts on
  native, Codex, OpenCode, or Bridge — is still pending. This release proves
  the portable core and repository scan, not restart/runtime adoption on any
  host.

## Release boundary (0.2.11)

- `0.2.11` wires the durable Context Plane into native, Codex, and OpenCode
  delivery and makes project context keyed. Repositories now receive explicit
  per-fact selectors with source-kind defaults plus an optional
  `.mssr/context-messages.json` manifest, a canonical-only modular
  `.mssr/project-context.json` loader with semantic knowledge topics, and a shared
  `loadProjectContextHost` that resolves one advisory plane
  (`projectContext`, `contextMessages`, `inbox`, `repository`) on route and
  bootstrap with an explicit `mssr_context_ack`.
- Six-probe activation evidence moves from 0/8 before this release to passing
  targeted activation tests (native/Codex/OpenCode route and bootstrap plus
  explicit ack), including an unrelated-domain negative.
- Historical 0.2.11 note: Bridge delivery was deferred at that release because the local dependency junction crossed the OpenCode workspace authority boundary. That adoption gap is closed as of 0.2.18: Bridge consumes the packaged MSSR artifact, live/restart readback is verified, and project-control behavior is inherited from the portable contract rather than an in-place junction.

## v0.1 — independent foundation

- [x] Independent repository, package metadata, routing contract, and docs.
- [x] Transversal agent bootstrap and idempotent `AGENTS.md` installer.
- [x] Advisory protocol for intent, phases, signals, re-planning, and chaining.
- [x] Complete extraction of the existing deterministic router into `src/`.
- [x] Keep Bridge as a thin adapter and compatibility surface.

## v0.2 — registry reliability

- [x] Provider interface plus a filesystem provider for local/system/plugin
  skills; generic providers can supply MCP catalogs and host capabilities.
- [ ] Native MCP `tools/list` / `listChanged` provider and host-provided
  capabilities.
- [x] Concurrent refresh single-flight, immutable snapshots, provenance, health,
  and stale-but-usable degradation.
- [x] Search/inspect APIs and routing fixtures for missing capability and
  additional-tool chains.
- [ ] Provider TTL policy and change notifications.

## v0.3 — optional MCP facade

- [x] Standalone `mssr-mcp` process exposing registry status/refresh, search,
  inspect, plan, and audit tools.
- [ ] Dynamic provider registration without making MSSR an execution proxy.
- [x] Bundled first-party provider discovery for the native facade, Codex-local,
  and OpenCode-local adapters, plus opt-in Codex junction installation. The
  manifest reserves five canonical names and a divergent external shadow blocks
  the audit.
- [x] Complete Bridge 0.6.88 runtime restart/catalog/version readback.
- [ ] Complete ChatGPT Web runtime validation; source and local Bridge proof do
  not establish every external host path.

## v0.4 — activation observability and durable learning

- [x] Bridge `trace-contract-v1`: local session continuity plus bounded process-shared recovery for stateless calls, unique-candidate propagation through direct and delegated calls, ambiguity protection, required-skill tracking, verification/persistence/outcome closure, and a multi-session MCP regression.
- [x] Logical observability epochs with a clean active baseline and preserved all-history telemetry instead of destructive metric deletion.
- [ ] Host compliance coverage proving that every eligible substantial task—not only traces already observed—loaded project context, produced a Routing Evidence Checkpoint, and invoked MSSR.
- [x] Privacy-preserving Bridge observatory for route plans, skill loads, context sources, verification, persistence, outcomes, user corrections, and friction signals without raw prompts or transcripts.
- [x] Transversal outcome attribution with one primary skill, supporting-skill contributions, latest-outcome-per-trace deduplication, success/acceptance rates, normalized scores, and an initial local dashboard.
- [x] Portable host-gated skill selection contract: deterministic recommendations remain metadata, required skills remain obligations, optional candidates can be explicitly `accepted`/`skipped` before context loading, and decision telemetry is analyzable by skill plus canonical semantic task signature.
- [x] Portable trace close preflight with `closureDue`, explicit missing gates / next action, ephemeral `until-outcome` working metadata, outcome purge, and regression coverage for the stale `maintenanceRevision` defect.
- [x] Bridge/ChatGPT Web adapter integration for host-gated bootstrap, temporary trace working metadata, closure notices, dashboard decision metrics, and outcome-time purge.
- [x] Trace integrity hardening follow-up — regressions discovered after the 0.2.43 C2f close are fixed at their owning layers: closed traces no longer project persisted outcomes as pending, and host recovery/evidence attribution preserves project/workflow ownership instead of merging unrelated work.
  - [x] Portable closure projection slice (0.2.44) — once an outcome checkpoint closes the lifecycle, `closure.obligations[outcome]` projects `complete` while `canCloseSuccess` remains a prospective pre-outcome gate; exact pure-contract and Codex-adapter regressions preserve `nextRequiredAction=none`.
  - [x] Bridge attribution slice (Bridge 0.6.109/0.6.110) — project/workflow ownership scopes implicit trace recovery, session identity remains correlation only, supporting repositories remain explicit `related_project` evidence, and two-project/runtime regressions prove unrelated ownership is not merged into one logical trace.
- [x] Privacy-bounded `learning-digest-v1`: before ephemeral RAM is purged at outcome, hosts may distill only canonical semantic signature, recommended/loaded/accepted-skipped skills, stage-to-skill transitions, context-module decisions, outcome metadata, and evidence-backed supported/rejected findings. `workingSummary`, active hypotheses, transcripts, prompts, secrets, and private reasoning are excluded by schema.
- [x] Exact-signature historical priors with minimum evidence threshold for skill activation/acceptance, stage-to-skill transitions, and selective skill/project context. Current mode is explicitly `observe-only`: priors are empirical observability outputs (`prefer` / `neutral` / `deprioritize` / `insufficient-evidence`) and do not affect deterministic routing, scores, context loading, or permissions.
- [x] Hard observe-only boundary: `routingInfluence=false`; current learning analysis has no consumption path from priors into deterministic route scoring or context selection. The minimum-evidence threshold is eligibility to analyze a pattern, not permission to activate it.
- [ ] Learning gate A — representative data collection: accumulate strict digests across domains, stages, callers, skill revisions and outcomes; track independent trace support, explicit-vs-default optional decisions, signature coverage and context-module coverage without changing runtime decisions.
- [ ] Learning gate B — dataset-quality audit: separate deliberate `accepted`/`skipped` evidence from `not-evaluated`, detect incomplete/ambiguous outcomes, retries/correlated traces, stale skill/routing revisions and host/model/runtime drift, and keep legacy selection telemetry distinct from strict learning digests unless explicitly migrated.
- [ ] Learning gate C — historical replay/holdout evaluation: using only evidence available before each historical decision, compare learned predictions against the unchanged deterministic baseline for activation precision/recall where measurable, over-activation/false-skip rates, required-load misses, outcome/acceptance lift, context cost/quality and calibration.
- [ ] Learning gate D — confidence calibration: replace raw empirical rates with support-aware bounded estimates before any decision use; evaluate smoothing, uncertainty intervals, minimum distinct-trace support, recency decay/staleness and sparse-signature back-off. Exact signatures remain the baseline; vector/semantic-neighbor retrieval is secondary and separately evaluated.
- [ ] Learning gate E — shadow decision model: compute what historical evidence *would* have suggested on new traces while continuing to execute the existing deterministic/host-gated route. Measure counterfactual disagreements and future predictive value without affecting skills, scores or context.
- [ ] Learning gate F — reviewed bounded activation, future only: only after replay plus shadow evidence shows repeatable net benefit, add an explicit versioned feature flag, bounded secondary historical score, exploration floor, explanations and instant rollback. Workflow-required skills, dependency invariants, safety/permissions and deterministic hard rules remain authoritative.
- [ ] Gate context-ranking influence separately from skill-ranking influence; module frequency alone must not count as usefulness without downstream outcome/evidence support.
- [x] Operational Notice Plane ownership: portable MSSR owns host-neutral attention/transition semantics while Bridge/other hosts own observation I/O and delivery. Trace, outcome, Context Message, and operational notice remain separate contracts; notices are advisory and never authorize actions. See `docs/OPERATIONAL_NOTICE_PLANE.md` and ADR 0002.
- [x] Operational Notice Plane gate A (0.2.19): pure `ok < watch < review < error` transition evaluator with REVIEW as the default attention threshold, stable-state suppression, bounded fingerprints, opened/changed/escalated/deescalated/resolved events, and explicit no-authority semantics.
- [x] Operational Notice Plane gate B first host slice: Bridge keeps its existing `bridgeNotices` queue/TTL/history/automatic-response drain and applies the portable transition policy to Skill Health and Project Context Health rather than creating another notice transport.
- [x] Operational Notice Plane gate C1 (0.2.20): portable operational projections for lifecycle idle/missing-outcome, project-knowledge maintenance debt, and Context Plane freshness. Host timers remain host-owned; `conflicting -> ERROR`, `stale/unavailable -> REVIEW`, `unknown -> WATCH`; maintenance attention resolves only after maintenance closes the current lifecycle revision and reopens after later invalidation.
- [x] Operational Notice Plane gate C2a (0.2.21): portable infrastructure correlation (`tunnel`, runtime continuity, restart state, transport symptom) plus provider/target health projections. Transport response loss alone is WATCH rather than proof of failure; response loss plus independently proven restart becomes REVIEW; provider availability remains distinct from target continuity/readiness.
- [x] Operational Notice Plane gate C2b (0.2.22): portable routing-compliance projection over route/trace/boundary/required-skill/required-phase evidence with quiet ordinary WATCH, substantial/missing/ambiguous/required-skill REVIEW, attribution/invalid-outcome ERROR, exact required-skill recommendations, stable semantic fingerprints, and explicit resolution. Optional selected-but-not-loaded skills remain non-obligations.
- [x] Operational Notice Plane gate C2c (0.2.23): portable consistency projection over bounded semantic claim keys with explicit `canonical` / `replica` / `historical` authority roles, independent freshness-vs-current-consistency semantics, value/revision/availability mismatches, release-critical escalation, role-aware recovery recommendations, deterministic fingerprints, and one identical `mssr_consistency_evaluate` contract on native/Codex/OpenCode. Historical receipts may remain valid history while stale for current operation; free-form memory is never heuristically rewritten.
- [x] Operational Notice Plane gate C2d (0.2.24): portable `evidence-first-v1` decision support layered over C2c diagnosis. Recommendations are ranked/deferred with explicit dependencies, confidence, information gain, risk, reversibility, cost, blast radius and score breakdown; hard source-of-truth gates outrank numeric score, empty evidence may abstain, and no recommendation authorizes execution. Future learned calibration remains behind replay/holdout, calibration, shadow, feature-flag and rollback gates.
- [x] Operational Notice Plane gate C2e-A/B (0.2.25): portable Situation Model normalizes bounded project-knowledge/runtime observations before C2c/C2d, distinguishes `observed` / `declared` / `inferred` / `learned` evidence, forbids inferred/learned canonical authority, classifies notices by category/class/priority while reusing the existing attention levels, and turns current repository revisions plus selected/delivered Context Plane receipts into cross-host stale-context evidence without parsing arbitrary prose.
- [x] Operational Notice Plane gate C2e-C (Bridge 0.6.105): Situation Model is adopted as the managed-project watcher over active Context Plane delivery receipts versus current canonical PROJECT_CONTEXT/PROJECT_MEMORY/PROJECT_STATE/changelog/ADR revisions. Stale delivered knowledge emits transition-driven classified `bridgeNotices` with metadata-only cross-restart suppression/resolution through the existing notice transport; no second queue and no free-form prose-to-truth inference. Live Bridge 0.6.105 runtime/tunnel/catalog readback is verified; consuming-host full release verification remains host-owned.
- [x] Operational Notice Plane gate C2e-D (0.2.27): strict explicit semantic claim producers normalize already-structured `release-version`, `state-value`, `ownership`, and `decision-revision` facts into the existing Situation Model. Closed source kinds deterministically map to C2c role, Situation category, and observed/declared evidence class while authority stays explicit; bounded scalar/revision validation prevents prose blocks or unknown evidence from inventing comparable truth. Existing C2c/C2d/Notice Plane remain the sole consistency/recommendation/attention owners.
- [x] Operational Notice Plane gate C2e-E (0.2.28): active Situation/C2d context attention now projects bounded advisory context-refresh requests from canonical `sourceRef` evidence into `.mssr/project-context.json`. A unique source path (or explicit `#section` selector) may name one exact core/module plus its existing bounded `maxChars`; shared-file ambiguity, unindexed authorities, or missing canonical source identity abstain to authority-only/unresolved guidance. The Context Plane host exposes this feedback without auto-loading it, rerunning semantic retrieval, changing budgets, or bypassing host choice/permissions.
- [x] Operational Notice Plane gate D (0.2.29): native, Codex and OpenCode expose the same strict `mssr_operational_notice_evaluate` contract over the shared portable transition evaluator. Cross-host tests require identical input schemas and byte-semantic decisions/candidates for opened/changed/escalated/deescalated/resolved, stable actionable suppression, quiet WATCH and explicit WATCH opt-in. The contract evaluates only; queueing, TTL, push/piggyback, history, UI and host-specific executable suggestions remain outside MSSR candidate parity and host-owned.
- [x] Operational Notice Plane gate E — `MssrNotice v1` portable semantic protocol: the host-neutral semantic notice identity is explicit/versioned, delivery metadata remains host-owned, Bridge relay adoption preserves the nested portable payload without normalization, native/CLI/Codex/OpenCode have an independent direct-host boundary, and the final Bridge 0.6.107 / packaged MSSR 0.2.32 migration matrix proves MSSR/native/external identities and one existing host queue without granting execution authority.
  - [x] E1 — semantic envelope (0.2.30): strict `mssr-notice-v1` now defines bounded schema version, stable lifecycle identity, kind/origin, attention level, severity, code/source/subject/message, recommendation, event evidence, collision-safe SHA-256 dedupe identity and `advisoryOnly`. Small single-line fingerprints remain inline; oversized/non-single-line producer fingerprints are represented in the envelope as `sha256:<length>:<digest>` while the transition evaluator still compares the original semantic fingerprint. Native/Codex/OpenCode prove identical payload semantics and host delivery metadata remains excluded.
  - [x] E2 — semantic/delivery separation (0.2.31): `MssrNotice` now has a deterministic validated semantic serialization/equality boundary, and contract tests prove that host wrappers may add/change queue ids, TTL/timestamps, attempts, delivery state, history/UI/actions or reorder input fields without changing the portable notice. The strict schema rejects those delivery fields both top-level and inside `details`; no notice queue, sink, retry spool or universal delivery runtime was added to MSSR.
  - [x] E3 — Bridge relay adoption (Bridge 0.6.106 / packaged MSSR 0.2.31): `BridgeNotice` remains Bridge's native delivery envelope/queue, while a genuine `MssrNotice` is preserved as a separately validated `mssrNotice` payload with its portable lifecycle identity unchanged. Bridge queue ids, TTL/timestamps, occurrences, UI/details mirrors and executable suggestions stay host-owned; foreign/native notices remain source-attributed and are never normalized into synthetic MSSR semantics. Live watchdog adoption proved `WATCH -> RESOLVED/OK` with stable `MssrNotice.noticeId`, one existing Bridge queue, 156 tools and no second semantic transport.
  - [x] E4 — direct-host parity (0.2.32): native/CLI-style hosts use `deliverMssrNoticeV1(...)` directly and Codex/OpenCode inherit the same explicit boundary through `MssrAdapter.noticeDelivery` / `deliverNotice(...)`. Host receipts are opaque and may differ freely while the validated portable notice remains identical; missing delivery configuration fails closed and transport failure is surfaced once without portable retry policy. No queue, spool, scheduler, UI/action runtime or new MCP tool was added.
  - [x] E5 — migration/integration gates (Bridge 0.6.107 / packaged MSSR 0.2.32): the final invariant matrix preserves existing Bridge-native notices, exact nested `MssrNotice` semantics, generic external-MCP identity without false MSSR normalization, stable portable dedupe with host-only occurrence metadata, exclusion of queue/TTL/action/execution fields from semantics, inert host actions, one existing Bridge queue, and independent E4 direct-host delivery.
- [x] Situation Model gate C2f — Architecture Impact / Drift Links (0.2.33–0.2.43): portable MSSR now owns the opt-in declared relationship layer, host-neutral observation/evidence, reviewed coarse projection, non-widening structural map, Markdown anchors, symbol fingerprints/refinement, derived graph candidates, declared executable invariants, bounded architecture-context feedback, and fail-open reviewed-current receipt semantics. A linked or structurally changed source remains possible impact only; no inferred relation, receipt, notice, or host observation may rewrite canonical architecture.
  - [x] C2f-A — explicit link contract (0.2.33): strict opt-in `.mssr/architecture-impact.json`, separate from `.mssr/project-context.json`, maps stable `architectureId` to canonical `authorityRef`, optional already-indexed `contextRef`, and bounded declared exact `impactRefs`. V1 rejects absolute/traversing/backslash/fragment/glob refs, duplicate architecture ids and duplicate refs within an entry; shared implementation refs across architectures are valid. The repository now self-hosts declared `architecture-impact-plane` and `project-context-plane` relations. Inferred/import/call-graph/model candidates remain non-authoritative until deliberately reviewed into the manifest. C2f-A performs no observation, possible-impact projection, notice, context injection or receipt persistence.
  - [x] C2f-B — host observation / portable evidence (0.2.34): portable MSSR deterministically plans the exact declared authority/source refs but performs no filesystem I/O; each host must return one explicit `available`+opaque revision, positively `missing`, or `unavailable` observation per ref. MSSR rejects wrong/extra/duplicate/omitted evidence, restores manifest order, keeps relationshipClass=`declared` separate from evidenceClass=`observed`, and can map one architecture's metadata into bounded Situation observations. Host failures propagate once and are never synthesized into evidence. B does not compare against a reviewed baseline, compute a source-set fingerprint, classify `possible-impact`, emit a notice, request context, or persist receipts; those semantics begin in C2f-C.
  - [x] C2f-C — architecture-impact projection (0.2.35): pure reviewed-baseline comparison over normalized C2f-B evidence. A reviewed baseline requires explicit confirmation, an available architecture authority and fully comparable implementation evidence; portable MSSR keeps architecture-authority revision separate from the declared implementation source-set fingerprint, then projects `aligned`, `possible-impact`, or `unresolved` with deterministic SHA-256 fingerprints and operational `ok`/`review` semantics. Verified authority/source revision or availability changes yield possible impact only; changed declarations or unavailable evidence remain unresolved. The projection is ready for the existing Operational Notice transition evaluator but C2f-C itself emits no notice, creates no watcher/queue, requests no context and persists no receipt. Source change and architecture change remain distinct facts.
  - [x] C2f-C.5 — Architecture Impact Map / Structural Fingerprints (0.2.36–0.2.41): coarse file-level impact remains the first filter, while portable MSSR adds exact pre-edit reverse mapping, sparse Markdown section anchors, host-neutral code-symbol analysis/fingerprints, reviewed structural refinement, non-authoritative graph candidates and declared executable invariants. Structural evidence may reduce byte-only noise or strengthen review evidence but never widens declared architecture or proves architecture changed.
    - [x] C2f-C.5-A — touch-time reverse map + structural refinement contract (0.2.36): optional `.mssr/architecture-structure.json` may refine only already-declared `architectureId`/`authorityRef`/`impactRefs`; it cannot create architectures or widen implementation authority. Exact touched-file lookup works before any hash/revision change and returns every surrounding declared architecture, preserving shared-file multiplicity plus exact `authorityRef`/`contextRef` and any declared structural selectors.
    - [x] C2f-C.5-B — Markdown architecture anchors (0.2.36): sparse stable `<!-- mssr-arch-anchor: ... -->` markers attached to headings define bounded architectural sections with deterministic semantic fingerprints. Changes outside the selected section and root-heading renames with the same stable anchor/body do not invalidate it; body/nested-subsection changes do. Self-hosted ADR 0001/0005 anchors prove the model without paragraph-by-paragraph links.
    - [x] C2f-C.5-C — code-symbol structural analysis: declaration, observation and comparison remain separate portable layers; parser/tool availability never grants canonical authority and Bridge-only graph/AST tools are never semantic owners.
      - [x] C2f-C.5-C1 — host-neutral analysis plan/evidence contract (0.2.37): portable MSSR deterministically plans only reviewed symbol selectors, accepts one explicit `observed` / `missing` / `unavailable` result per selector, binds evidence to the host-observed `sourceRevision`, keeps `analyzerId` separate from `fingerprintScheme`, restores declaration order, rejects extras/duplicates/omissions/undeclared targets, and propagates analyzer failure once without retry or synthetic evidence. C1 performs no filesystem read, AST parsing, structural comparison, possible-impact refinement, notice, context request or receipt persistence.
      - [x] C2f-C.5-C2 — portable/reference AST analyzer capability (0.2.38): `analyzeArchitectureTypeScriptSource` consumes host-supplied source text + source revision behind the C1 callback contract, dynamically loads or accepts an injected TypeScript Compiler API, and emits the existing strict C1 evidence with exact compiler-version fingerprint scheme `mssr-ts-symbol-v1:ts<version>`. Semantic token canonicalization ignores whitespace/comments while preserving signatures, executable bodies/initializers and type/class shape independently; parser absence/failure/invalid syntax stays explicit `unavailable`. TypeScript remains outside runtime dependencies and Bridge-only AST tools remain optional host implementations, not semantic owners. C2 performs no baseline comparison or `possible-impact` refinement.
      - [x] C2f-C.5-C3 — structural comparison/refinement policy (0.2.39): an explicitly reviewed structural baseline binds sparse Markdown/code landmarks to the same coarse reviewed baseline and source revisions. For refs already changed by C2f-C, compatible unchanged structural fingerprints refine attention to `noise-candidate`/WATCH without erasing the coarse `possible-impact`; changed landmarks remain `structural-possible-impact`/REVIEW; changed structure declarations, unavailable/missing/stale evidence, availability transitions, absent landmarks or fingerprint-scheme mismatch remain `unresolved`/REVIEW. C3 emits no notice/context request/receipt and never claims architecture changed.
    - [x] C2f-C.5-D — derived graph candidates + executable architecture invariants (0.2.41): portable MSSR normalizes host-observed import/call/dependency edges only from already-declared architecture refs into deduplicated `derived` candidates with `promotionState=candidate` and `canonicalReviewEligible=false`, so nearby targets never widen canonical scope. Optional `.mssr/architecture-invariants.json` declares strict `require-edge`/`forbid-edge` invariants; complete evidence may prove absence while partial evidence never does, observed forbidden edges or complete missing required edges produce REVIEW/`invariant-violation`, and all evaluations keep `canonicalRewriteAllowed=false`. MSSR self-hosts the boundary with a declared no-host-dependency invariant over Architecture Impact projection.
  - [x] C2f-D — bounded architecture context feedback (0.2.42): active Architecture Impact REVIEW now resolves at most the declared `contextRef` plus architecture authority through the existing C2e-E exact-resolution helpers on the next natural MSSR replan. The feedback is advisory-only, `required=false`, `replanOnly=true`, keeps `semanticRetrievalRerun=false`, `autoLoad=false`, `budgetOverride=false`, and preserves normal budgeted selection; aligned architecture emits no request. MSSR self-hosts the contract against its real Architecture Impact + project-context manifests.
  - [x] C2f-E — reviewed-current receipts and explicit correction (0.2.43): portable MSSR defines strict metadata-only `reviewed-current` receipts for host-local persistence under ignored `.mssr/runtime/`. Receipt creation requires an explicit resolved review over complete active `possible-impact` evidence; only an exact match of architecture id, declaration fingerprint, reviewed baseline, current authority revision, current source-set fingerprint, full projection fingerprint and receipt integrity suppresses the same repeated review. Missing/corrupt receipts, incomplete evidence or any identity change fail open to REVIEW. Receipts never edit canonical ADR/context/link truth; loss only causes safe re-review.
- [ ] Architecture Impact host adoption gate — wire the completed portable C2f contracts into real host edit/replan lifecycles without making any host the semantic owner. A host may provide touched refs, source revisions, optional AST/graph observations, and local reviewed-current persistence; MSSR must perform the same reverse-map/refinement/context-feedback decisions for native, Codex, OpenCode, and Bridge inputs. Prove adoption per host with package/version + restart/readback and parity tests. Do not require an always-on filesystem watcher, do not inject architecture on every tool response, and do not treat Bridge-only metadata as portable truth. Bridge 0.6.110 now consumes exact MSSR 0.2.45, but package parity alone does not prove end-to-end C2f lifecycle adoption.
  - [x] Host adoption A — portable lifecycle coordinator: exact touched refs resolve only declared affected architectures and their existing C2f-B observation plans; host evidence then flows through the existing C2f-C projection, C2f-E reviewed-current suppression, and C2f-D natural-replan context feedback with `semanticOwner=mssr`, no I/O/watcher/autoload, and `canonicalRewriteAllowed=false`.
  - [x] Host adoption B — compose optional structural/AST/derived-graph/invariant evidence through the same coordinator (0.2.47): structural fingerprints refine coarse `possible-impact` into WATCH/REVIEW without erasing the coarse fact; derived graph remains candidate-only and never raises canonical attention; declared invariant evidence may raise WATCH/REVIEW and cannot be silenced by a coarse reviewed-current receipt; analyzers remain optional host capabilities and `canonicalRewriteAllowed=false`.
  - [ ] Host adoption C — prioritize Bridge/ChatGPT Web first: wire Bridge to the portable lifecycle, adopt the current packaged MSSR version, prove touched-ref -> observation -> structural/invariant aggregation -> reviewed-current/context-feedback end to end, then perform controlled restart + live version/catalog/readback. Bridge remains an observation/persistence adapter; MSSR remains the semantic owner.
  - [ ] Host adoption D — wire Codex next to the same portable lifecycle and prove behavioral parity against the verified Bridge path without depending on Bridge-only metadata or tools.
  - [ ] Host adoption E — complete native + OpenCode lifecycle adoption and final cross-host parity over the same portable coordinator, including equivalent REVIEW/WATCH decisions, receipt invalidation, context feedback, and no canonical auto-rewrite.
- [x] Documented MSSR Context Plane v1 ownership and safety contract: portable
  selection/continuity/message semantics; repository-owned facts; adapter and
  provider-owned I/O/delivery; provenance/freshness receipts; and
  review-only persistence proposals. This is an ADR/documentation boundary,
  not a claim of universal runtime message support.
- [x] Context Plane gate A — versioned portable message and
  continuation-receipt fixtures and cross-host selection parity, including
  source class, canonical owner, provenance, freshness, trace association,
  privacy bounds, and no-authority invariants (0.2.9).
- [x] Context Plane phase 2 — portable strict producers, bounded repository
  collector (ADR/incident/changelog/PROJECT_* facts plus supplied Git/provider
  receipts), evidence freshness revalidation, and a durable explicit-ack
  advisory-only JSON inbox (0.2.10).
- [x] Context Plane gate B (native, Codex, OpenCode) — adapter delivery: one
  shared host helper drains the durable inbox on native/Codex/OpenCode route
  and bootstrap, returns `projectContext`/`contextMessages`/`inbox`/
  `repository`, and exposes explicit `mssr_context_ack`, without assuming
  server push creates a host turn and preserving `unknown`/`stale`/
  `unavailable` rather than fabricating facts (0.2.11).
- [x] Context Plane gate B (Bridge/ChatGPT Web) — Bridge consumes packaged MSSR artifacts and delivers selected Context Messages plus the durable `.mssr/runtime/context-inbox.json` plane through the same host adapter contract. Source/runtime adoption is proven separately by package version and live restart/readback rather than inferred from the sibling workspace.
- [ ] Context Plane gate C — expand continuation/persistence-proposal conformance across native, Codex, OpenCode, and Bridge/ChatGPT Web. A proposal must remain reviewable repository-owned work and never auto-write memory, skills, routing, or changelogs.
- [x] Canonical modular project-context contract plus Bridge adapter: `.mssr/project-context.json` is the single active manifest, minimal core loads first, stage/intent selects bounded context/memory/state/directive modules, and missing/invalid initialization is explicit health evidence rather than a legacy/full-document fallback.
- [ ] Project Memory reference-backed storage parity — make physical memory storage match the already-modular semantic selector instead of letting optional memories accumulate as sections in `PROJECT_MEMORY.md`. Keep `.mssr/project-context.json` as the only manifest; no parallel memory index.
  - [x] Gate A — portable contract/early attention: `PROJECT_MEMORY.md` is core-oriented, two or more optional `kind=memory` modules backed by that root produce `root-backed-memory-fanout`, and the read-only modularization planner proposes exact ref extraction even for small sections while preserving an existing single-section module id/kind/selectors. Regression proves selected module bytes/ids remain equivalent after the physical move.
  - [ ] Gate B — self-host/workspace migration: MSSR self-host and Bridge are migrated and parity-verified. Bridge moved six indexed optional memories with exact ID/selector/payload preservation, reducing its root from 16850 to 12719 bytes and Project Context Health from WATCH to OK. A read-only `D:\\Dev` scan still finds legacy root-backed memories in other repositories; most are a single bootstrap `project-decisions`, while `MyceliumFront`, `pz-furniture-profiler`, and `zomboid-smartwatch-network` have multi-memory fanout. Those repositories require per-project concurrency/semantic review rather than mass mutation.
  - [x] Gate C — host writer UX: reference-backed capture is the normal path for new optional memory; Bridge rejects creation of a new optional root-memory section and directs callers to `project_context_capture`, while deliberate core/cross-area root edits and maintenance of existing legacy refs remain possible. A conservative host migration script now defaults to check-only, requires exact safe candidates for apply, preserves payload/selector identity, aborts on target collision or concurrent source change, and never performs background migration or semantic rewrites.
- [x] Critical cross-cutting Project Context applicability (0.2.49): repositories can explicitly mark bounded rules such as localization/encoding, packaging, trust-boundary, persistence, or runtime payload invariants as conditionally required for matching mutations (`requiredWhen`) before semantic ranking, while preserving read-only selectivity, artifact/stage scoping, explicit required-budget debt, and no automatic inference/promotion from prose.
- [x] Portable versioned change-history contract: strict `changelogs/X.Y.Z.md` parser, PROJECT_CONTEXT/PROJECT_MEMORY/PROJECT_STATE impact declarations (`updated` / `reviewed-none` / `pending`), pure consistency evaluator, and deterministic history-loading predicate for debugging/recovery/release work.
- [x] Bridge project-authority/change-consistency host implementation: recursive workspace audit uses portable MSSR initialization/health semantics and persist-mode Git/changelog/memory drift can block publish readiness without auto-writing memory.
- [x] Keyed project-context resolution on native, Codex, OpenCode, and packaged Bridge paths uses the same `.mssr` host helper, semantic selectors, repository facts, and durable runtime inbox; `.bridge` is not active MSSR project authority.
- [x] Workspace bootstrap/health rollout: the real `D:\Dev` managed workspace is initialized under the canonical `.mssr` contract (22/22 discovered managed Git repositories on the verified 0.2.18 pass), with structural WATCH/REVIEW kept advisory rather than treated as failed initialization.
- [ ] Post-iteration learning/promotion pipeline: isolated evidence -> project regression/context-memory-state update, architecture/design decision, owning skill/tool/guide update, or MSSR fixture/metadata correction according to ownership; never assume every lesson belongs in a skill.
- [ ] Operational Pattern Automation gate — promote repeated deterministic maintenance/release/tool sequences into bounded automation candidates instead of making every agent replay ceremony manually. Trace/digest evidence may propose the candidate; it never auto-mutates source or promotes a capability.
  - [x] Policy/owner slice (0.2.39): `skill-maintenance-loop` owns detection and classification. Require repeated equivalent evidence plus stable inputs/outputs, deterministic ordering, objective gates, safety boundary and fail-closed behavior; prefer an existing owner and automate only mechanical work/receipts, never architecture truth, approval or semantic persistence decisions.
  - [x] First proving implementation (0.2.40) — MSSR `release:gate`: collapse repeated verify/package/hash/readback bookkeeping into one deterministic script receipt. It validates already-reviewed changelog/index/current-release metadata, runs one full verify then one final pack, independently reads bytes/SHA-256, and atomically writes `.mssr/runtime/releases/<version>.json`; short `pkg:<version>#<8hex>` aliases are display-only. It never chooses project-knowledge status, publishes, commits/pushes, or adopts host packages.
  - [ ] Release receipt provenance/doc projection — let a host optionally attach bounded trace/workflow/evidence provenance to the ignored runtime release receipt and emit one deterministic doc-ready summary (`alias`, version, bytes, full SHA-256, receipt path) so agents do not repeatedly transcribe hashes. Provenance remains host metadata; generated summaries/patch proposals may assist maintenance but must never auto-write PROJECT_*, changelogs, ADRs, or semantic authority.
  - [ ] Learning integration — detect privacy-safe repeated step signatures across traces/digests and emit `add-script-tool-or-guide` candidates with evidence/count/owner; require normal human/source review and regression before creation or promotion.
- [ ] Feed eligible historical priors back into route/context ranking as a bounded secondary score with an exploration floor, decay/staleness policy, and review-only proposals before durable routing semantics change. Exact deterministic metadata/fixtures remain authoritative; evaluate vector similarity only as a secondary retrieval signal for nearby signatures.
- [x] Established one canonical first-party MSSR skill source inside this package:
  `mssr-agent-routing`, `shared-skill-governance`,
  `skill-routing-maintainer`, `skill-maintenance-loop`, and on-demand
  `mssr-observability-maintenance`. The five directories exactly match the
  manifest and preserve explicit activation metadata; that does not make every
  listed skill required for every task.
- [x] Met the source admission gates: canonical owner/provenance manifest,
  bundled package inclusion, frontmatter/manifest conformance, provider
  precedence, reserved-shadow audit failure, opt-in installer, and custom
  catalog guards.
- [x] Added source conformance for native, Codex-local, and OpenCode-local
  discovery plus Codex mounting. External, user-created, project, and plugin
  skills remain separate catalogs and cannot silently shadow a reserved name.
- [ ] Complete the runtime conformance matrix: Bridge live discovery after
  restart is proven; ChatGPT Web discovery plus lifecycle, digest, replay, and
  rollback parity across all hosts remain. Source tests do not prove those
  broader gates.
- [ ] Historical replay benchmark measuring missed activation, over-activation,
  required-load compliance, phase continuity, latency, recovery, and outcome
  quality on confirmed real cases.
- [ ] Prove the same trace/checkpoint contract through the packaged host
  adapters while preserving caller-owned execution; do not infer this from a
  Bridge-only integration release.

## v1.0 — portable distribution

- [ ] Published or packaged distribution with reproducible installation.
- [ ] Compatibility matrix and migration guide for a new machine.
- [ ] Evaluation suite measuring route precision, recall, latency, and safe
  degradation rather than relying on anecdotal success rates.
