{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mauroprime.local/schemas/skill-routing-fixtures.schema.json",
  "title": "MauroPrime skill routing fixtures",
  "type": "object",
  "required": ["schemaVersion", "cases"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "cases": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "task", "stage", "expect"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "task": { "type": "string", "minLength": 1 },
          "taskVariants": { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
          "context": { "type": "string", "maxLength": 4000 },
          "caller": { "enum": ["codex-local", "chatgpt-web", "other"] },
          "stage": { "enum": ["start", "implement", "verify", "persist", "close", "resume"] },
          "completedPhases": {
            "type": "array",
            "items": { "enum": ["discovery", "safety", "implementation", "verification", "persistence", "maintenance"] }
          },
          "sources": {
            "type": "array",
            "items": { "enum": ["codex-local", "codex-system", "codex-plugin", "roblox"] }
          },
          "intent": { "type": "object" },
          "expect": {
            "type": "object",
            "properties": {
              "classificationMode": { "enum": ["structured-semantic", "lexical-fallback"] },
              "contextUsed": { "type": "boolean" },
              "activeIncludes": { "type": "array", "items": { "type": "string" } },
              "activeExcludes": { "type": "array", "items": { "type": "string" } },
              "deferredIncludes": { "type": "array", "items": { "type": "string" } },
              "deferredExcludes": { "type": "array", "items": { "type": "string" } },
              "missingRequiredPhases": { "type": "array", "items": { "enum": ["discovery", "safety", "implementation", "verification", "persistence", "maintenance"] }, "uniqueItems": true },
              "agentFallbackPhases": { "type": "array", "items": { "enum": ["discovery", "safety", "implementation", "verification", "persistence", "maintenance"] }, "uniqueItems": true }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
