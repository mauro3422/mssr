# 0.2.13 - 2026-08-13

## Contract

- Summary: Godot routing tightening. `godot-graph-ux-audit` declared `coding` as a routing domain, so a generic repo/architecture verify intent (domains `coding`, `skill-system`) matched it on the shared `coding` domain and pulled `godot-project-inspection` in as its required dependency. Removing `coding` from `godot-graph-ux-audit.domains` narrows the skill to its `godot` identity; the existing deterministic domain gate (`skill.domains.length > 0 && !domainMatched`) now excludes the whole Godot block from non-Godot tasks. No new routing gate, schema field, or contract surface was added; this release only tightens the domain metadata of one skill.
- Areas: skill-routing, maintenance
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- **Domain metadata** (`config/skill-routing/skill-routing-overrides.json`): `godot-graph-ux-audit.domains` collapses from `["godot", "coding"]` to `["godot"]`. Together with the pre-existing scorer gate at `scoreEntry`, a non-Godot acceptance/architecture verify no longer selects the Godot graph or inspection skills.
- **Fixtures** (`config/skill-routing/skill-routing-fixtures.json`): `architecture-verify-excludes-godot` asserts the generic repo-architecture verify route excludes all four Godot skills; `godot-graph-ux-audit-verify` asserts the Godot graph verify route conserves `godot-project-inspection` plus `godot-graph-ux-audit`.
- **Host-gated regression test** (`scripts/test-skill-routing.mjs`): the optional-dependency route intent now targets the Godot graph editor so graph+inspection are selected on a real Godot task while the host-gated optionality contract is preserved.

## Verification

- `npm run check` and `npm run build` pass (no schema or code contract change; `require*` key set is unchanged).
- `npm run test:router` passes all fixtures including both new cases, and the host-gated optional dependency/additive-dependency checks still hold.
- `npm run audit:check` passes; overrides validate under the unchanged `skillRoute` definition.
- `git diff --check` confirms no whitespace errors in the release change set.