# 0.2.58 — Unreleased source correction

- Summary: Narrow the required visual-evidence lifecycle so a functional code audit cannot become a visual obligation merely because it reviews documents and needs approval.
- Areas: skill routing, workflow conditions, host-gated obligations, visual-evidence regression coverage
- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Adds `allNeeds` to routing conditions and explicit skill metadata. It requires every listed capability while existing `needs` retains its ordinary any-match behavior.
- Requires both `visual-qa` and `human-approval` for `visual-evidence-lifecycle` and the direct `visual-evidence-audit` candidate.
- Adds a structured nearby-negative fixture for the functional “Smoke Lab” pre-release code audit and verifies active, deferred, and host-gated contamination.

## Verification

- `npm run test:skill-routing` — PASS: 225 fixture cases, clean routing audit, no maintenance requirement.
- `npm run verify` — PASS: typecheck, portable/core/context/host suites, cross-host conformance, 225 routing fixture cases, `skill_route_audit`, and `audit:check` all pass.
- No package version bump, pack, Bridge adoption, or publication is claimed by this draft.

## Project knowledge impact

- PROJECT_CONTEXT: updated with the narrow conjunction boundary.
- PROJECT_MEMORY: updated with the visual-evidence activation decision.
- PROJECT_STATE: updated to record the unreleased source correction and unchanged published release.
