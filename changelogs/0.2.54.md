# 0.2.54 — 2026-08-22

- Summary: Close the source-level v1 reliability gates for dynamic providers, cross-host Architecture/Context conformance, observe-only learning evaluation, and compact first-party skill distribution.
- Areas: registry, providers, architecture-impact, context-plane, learning, skills, distribution, verification
- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Separates provider transport health from catalog freshness, adds bounded TTL expiry, immutable last-change receipts, subscriber notifications, coalesced list-change refresh, dynamic add/remove and repeated-cursor/page-count protection. Cached stale metadata remains planning evidence only; no provider exposes tool execution.
- Registers identical `mssr_architecture_impact_plan`, `mssr_architecture_impact_evaluate`, and `mssr_context_proposal_review` contracts on native, Codex and OpenCode. Hosts still own observation, explicit review and persistence; every proposal review forbids auto-write.
- Adds strict dataset audit, chronological replay/frozen holdout, Beta(1,1)+Wilson-95 calibration and counterfactual shadow evaluation. Every result fixes `mode=observe-only`, `routingInfluence=false`, and `automaticPromotionPerformed=false`; sparse or unmeasurable evidence abstains.
- Reduces all five first-party skill roots by 75–81% while retaining activation/invariants/workflow/verification in `SKILL.md` and moving situational procedure into 11 parent-owned selective references. Routing metadata and fixtures remain unchanged.
- Adds a reproducible, non-publishing v1 distribution/migration/evaluation guide and wrapper with package dry-run, safe opt-in junction migration, selective-context, routing and audit gates.

## Verification

- Full `npm run verify` — PASS, including 224 routing fixtures, 20 native MCP tools, Codex/OpenCode standalone adapters, provider degradation/concurrency, learning abstention and cross-host Architecture/Context parity.
- `pwsh -NoProfile -File scripts/verify-first-party-skills-v1.ps1` — PASS, including package dry-run, selective context, routing, audit and installer sandbox.
- Bridge paged-context lifecycle focal regression — PASS before packaged adoption: a completed page exposes an explicit checkpoint/close gate while `automaticCheckpoint=false` and `automaticOutcome=false`.
- `git diff --check` — PASS before release packaging.

## Project knowledge impact

- PROJECT_CONTEXT: updated with provider freshness, cross-host conformance and observe-only learning ownership.
- PROJECT_MEMORY: updated with first-party progressive-disclosure and learning activation decisions.
- PROJECT_STATE: updated with the 0.2.54 source/package/adoption gates.
