# 0.2.53 — 2026-08-22

- Summary: Restore duplicate suppression inside portable paged skill-context planning so core-covered selected modules do not consume budget or reappear in a later page.
- Areas: skill-context, context-budget, continuation, telemetry, verification
- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Restores the invariant that a selected module whose material is already contained in that skill's loaded core or earlier selected material is not emitted as another context unit.
- Marks deduplicated module decisions as `already-covered-by-loaded-context`, preserves their allocation tier, reports zero delivered module characters for that decision, and adds the avoided assembled characters to both per-skill and page-level `duplicateCharsAvoided`.
- Keeps the 0.2.52 page ordering, exact-once delivery for non-duplicate units, `required|accepted` distinction, opaque cursor validation, explicit continuation and indivisible-unit blockers unchanged.

## Verification

- `npm run check` — PASS.
- `npm run test:skill-context-loader` — PASS, including core-covered duplicate suppression/accounting together with 23,310-under-18,000 pagination, cursor tamper/staleness and oversized-unit coverage.
- Focused selective-context, Codex/OpenCode standalone and change-history/project-context checks are rerun before packaging by the coordinating host.
- Full `npm run verify` — PASS with 224 routing fixtures and `skill_route_audit.ok=true`. On the policy-restricted Windows host, `Set-Acl` is unavailable; the OpenCode salt regression now accepts only the existing bounded `mssr-opencode-windows-acl-unavailable` diagnostic, while still proving random durable correlation, secret non-disclosure and rejection of every unrelated diagnostic.
- `git diff --check` — PASS.

## Project knowledge impact

- PROJECT_CONTEXT: updated with the narrow covered-material deduplication invariant and ownership boundary.
- PROJECT_MEMORY: updated with the correction rationale and non-regression boundary.
- PROJECT_STATE: updated for source `0.2.53`, while preserving already-packaged `0.2.52` immutability and recording that 0.2.53 has not been packaged or adopted.
