# 0.2.6 — 2026-08-13

## Contract

- Summary: Add a portable change-history contract, project-memory consistency evaluation, observe-only learning documentation, and selective changelog loading signals for host adapters.
- Areas: learning, project-context, changelog, maintenance, routing
- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Added `change-history.ts` with a strict version-changelog parser, PROJECT_* impact statuses, selective history-loading predicate, and pure consistency evaluator.
- Added regression coverage for parsing, history selection, pending knowledge impact, version/index mismatch, and observed PROJECT_* updates.
- Documented the MSSR learning rollout as collection -> dataset audit -> replay/holdout -> calibration -> shadow -> bounded future activation; current historical influence remains zero.
- Expanded the roadmap with explicit learning gates and the planned MSSR first-party core skill package.
- Adopted versioned `changelogs/X.Y.Z.md` files plus an index while preserving the previous monolithic history in `changelogs/LEGACY.md`.

## Verification

- `npm run build` passes.
- `npm run test:change-history` passes.
- Full `npm run verify` is required before publication.
