# 0.2.45 — 2026-08-19

- Summary: Reconcile the published MSSR package with routing metadata already present in the verified Git source: `steam-workshop-publication` is now guaranteed to ship in the versioned artifact with explicit metadata, positive/negative coverage, and continuation coverage.
- Areas: skill-routing, release-integrity, packaging, host-adoption, project-state
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: reviewed-none
- PROJECT_STATE: updated

## Changes

- Publishes the already-reviewed `steam-workshop-publication` routing contract that was present in Git after the 0.2.44 artifact had been packed but was absent from that tarball.
- Preserves routing authority in `config/skill-routing/`; no Bridge-local exception or audit relaxation is introduced.
- The source routing suite proves 218 cases and 50/50 owned skills with explicit routing plus positive and negative fixture coverage before the release gate.
- This is a release-integrity reconciliation only: no new portable runtime feature, architecture ownership change, or learning-policy activation is introduced.
- Bridge adoption remains a separate host gate. Bridge 0.6.110 may consume this exact release artifact only after package hash/readback and host verification.

## Verification before release gate

- `npm run test:skill-routing` — PASS: 218 cases, 50 owned skills, 50 explicit routing entries, 50 positive-fixture coverage, 50 negative-fixture coverage, maintenanceRequired=false.
- Direct comparison proved the mismatch that motivated the release: Git source contains `steam-workshop-publication` in overrides/fixtures, while the immutable 0.2.44 tarball does not.
- Full source verification and exactly one final package are delegated to `npm run release:gate`.

## Project knowledge impact

- PROJECT_CONTEXT: reviewed-none; portable ownership and architecture boundaries are unchanged.
- PROJECT_MEMORY: reviewed-none; the durable rule already exists: routing authority is `config/skill-routing/` and hosts must consume versioned packaged MSSR artifacts rather than sibling working-tree state.
- PROJECT_STATE: updated to record the 0.2.45 reconciliation lifecycle and, after release-gate success, the exact immutable package receipt used by host adoption.
