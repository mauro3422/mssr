# 0.2.55 — 2026-08-22

- Summary: Complete first-party progressive disclosure by replacing the monolithic maintenance-friction reference with bounded semantic modules.
- Areas: skills, selective-context, maintenance, verification
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Replaces one 20,718-character `friction-patterns` context unit, previously selected by a generic anomaly signal alone, with a 1.3 KB index plus nine topic-owned references. Every friction reference remains below 5,000 characters.
- Gives detailed modules conjunctive stage/intent selectors for integration/release, skill runtime/routing, tooling/scale, verification/readback, visual payload integrity, learning decisions, hierarchical mutation identity, cross-project authority and close.
- Preserves the full durable pattern catalog while preventing `warning-observed` by itself from materializing every pattern.
- Adds package conformance regressions for bounded friction-module size, generic-warning selectivity and absence of indivisible budget blockers.

## Verification

- `npm run test:first-party-skills` — PASS.
- `npm run test:skill-routing` — PASS, 224 routing fixtures.
- `npm run audit:check` — PASS.
- Full `npm run verify` and `npm run release:gate` — required before publication.

## Project knowledge impact

- PROJECT_CONTEXT: reviewed; the existing selective skill-context ownership and whole-unit paging contract remain accurate.
- PROJECT_MEMORY: updated with the rule that a compact root is insufficient when a broadly selected reference remains monolithic.
- PROJECT_STATE: updated with the 0.2.55 package/adoption state.
