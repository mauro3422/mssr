# 0.2.71 — 2026-09-19

- Summary: begin R4 Deterministic Semantic Consistency by auditing the existing Situation/C2c/C2d, Context Message, freshness and host-conformance machinery; add a machine-auditable integration coverage inventory plus the first typed current-state contradiction slice without treating historical/superseded evidence or arbitrary prose as current truth.
- Areas: semantic-consistency, situation-model, host-conformance, project-context, roadmap, documentation, tests
- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Add a portable semantic-consistency coverage inventory that classifies contract/evaluator/producer/consumer/fixture/export/host-adoption edges as `implemented`, `host-supplied`, `reserved`, `pending-adoption`, or `unresolved` instead of inferring runtime wiring from tests or exports.
- Record the existing `roadmap-contradiction` and `unresolved-reference` Context Message kinds truthfully as reserved producer gaps: their contracts/consumers exist, but no production emitter is claimed yet.
- Extend explicit semantic claims with bounded `scope`, temporal `validity`, optional observation time and extractor provenance while preserving the existing default-project semantic key contract.
- Add deterministic temporal validity for `current`, `historical`, `superseded`, and `unknown`: historical/superseded evidence remains provenance but does not compete as current truth, while unknown validity abstains.
- Add narrow structured extractors for the repository's explicit `R<n>` ROADMAP checkboxes and machine-readable `mssr-state` markers. Surrounding prose is deliberately ignored and duplicate declarations fail closed.
- Add `evaluateMssrSemanticConsistency`, which filters temporal validity before reusing the existing Situation Model/C2c/C2d stack and emits typed bounded findings for current state/canonical/ownership/decision/release/runtime conflicts.
- Keep authority, temporal validity, scope and freshness separate: Document Freshness remains temporal/path evidence and is not widened into a prose-semantic engine.
- Reconcile durable MSSR project state with the already-verified R3 live adoption: R1-R3 are complete end-to-end, live Bridge `0.6.138` consumes exact MSSR `0.2.70`, and R4 is now the active portable program.
- Keep R4 project knowledge bounded without budget inflation: move detailed C2c/C2d/current-truth policy into selective module `mssr-semantic-consistency-decision`, reduce the monolithic `PROJECT_MEMORY`, and restore Project Context Health to `ok`.
- No Bridge release or R4 host-adoption claim is introduced by this package. Bridge remains an observation/transport adapter until a later explicit R4 adoption gate requires new wiring.
- No context-budget increase, permission widening, model/NLI truth inference, automatic documentation rewrite, or learned routing influence is introduced.

## Verification

- Add regression coverage for ROADMAP pending versus canonical PROJECT_STATE completed, stale HANDOFF-style temporal evidence through historical/superseded claims, canonical conflicts, runtime drift, contradiction resolution, scope isolation, duplicate declaration rejection, and self-host ROADMAP/PROJECT_STATE consistency.
- Gate A cross-host coverage remains machine-auditable and distinguishes source implementation from host-supplied/adoption state.
- Full `npm run verify` passes with R1 trace identity, R2 lifecycle activation, R3 continuation/context economy, Situation/C2c/C2d, project-context health, cross-host conformance and routing audit regressions intact before release preparation.
- `npm run release:gate` is the immutable final release gate and must independently rerun verification, package `0.2.71`, read back artifact bytes/SHA-256, and persist its bounded runtime receipt.

## Remaining R4 gates

- Gate B remains partial until reusable typed relationship/dependency edges are justified beyond the existing claim identity contract.
- Gate C remains partial beyond the narrow ROADMAP/state extractors; version/owner/decision extractors should be added only from explicit structured sources.
- Gate D remains partial beyond current exact value/revision, canonical conflict, scope and temporal-validity behavior.
- Gates E-H remain open: bounded candidate retrieval, production Context Message wiring (including `roadmap-contradiction`), host adoption/longitudinal QA, and any optional NLI shadow path.
