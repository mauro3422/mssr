# 0.2.15 - 2026-08-14

## Contract

- Summary: strengthens the existing `godot-scene-authoring` routing contract for human review/test preparation where the complete editor scene-tab set matters. A successful batch of scene opens no longer implies exclusivity; exact review preparation must verify the complete open-scene set and reject stale/extra tabs.
- Areas: skill-routing, Godot scene authoring, verification, maintenance
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- **Exact review-set fixture** (`config/skill-routing/skill-routing-fixtures.json`): adds a positive case for preparing only the requested Godot scenes, in order, with explicit real-state verification before human review.
- **Boundary preserved**: the existing read-only Godot inspection negative remains outside scene authoring, while the existing continuation fixture still covers resumable multi-scene opening.
- **Owner unchanged**: `godot-scene-authoring` remains a user-owned skill in `mauroprime-skills`; MSSR owns only the deterministic routing metadata/fixtures.

## Verification

- Routing suite passes with 208 cases and routing audit reports `maintenanceRequired=false`.
- `npm run audit:check` passes with no schema violations or blocking warnings.
- This package is intended to be vendored by MauroPrime Bridge so live routing consumes the same exact-review contract rather than the previous packaged fixture set.
